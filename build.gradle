apply plugin: 'java'
apply plugin: 'checkstyle'

checkstyle {
    toolVersion = 6.0
}

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}